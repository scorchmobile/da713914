<div class="container">
    <div class="row">
        <div class="col-sm-12">

            <h1>Editing Fact</h1>

            <br>
            <%= link_to (fa_icon('eye') + ' Show'), @fact, class: 'btn btn-lg btn-primary' %> |
            <%= link_to (fa_icon('backward') + ' Back'), facts_path, class: 'btn btn-lg btn-primary' %>
            <br><br>

            <%= render 'form' %>

        </div>
    </div>
</div>
